import 'package:flutter/material.dart';

class MySnack extends StatelessWidget {
  final String label;
  final String textWhat;

  const MySnack({
    Key? key,
    required this.label,
    required this.textWhat,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
